apiVersion: batch/v1
kind: Job
metadata:
  name: fly-train
  namespace: flyc4
  labels:
    app: flyc4
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 43200
  ttlSecondsAfterFinished: 7200
  template:
    metadata:
      labels:
        app: flyc4
    spec:
      # user-directed placement on k3s-w6; selection follows the platform rule
      # (hardware capability labels + gpu.present, never device-plugin overrides)
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - { key: kubernetes.io/hostname, operator: In, values: [k3s-w6] }
                  - { key: hardware, operator: In, values: [rtx3090-x1] }
                  - { key: nvidia.com/gpu.present, operator: In, values: ["true"] }
      containers:
        - name: train
          image: flyc4:latest
          imagePullPolicy: Never
          command: ["python", "-m", "flyc4.train",
                    "--batches", "1177", "--games", "1024", "--steps", "48", "--lr", "3e-2",
                    "--eval-every", "25", "--save-every", "10",
                    "--out", "/out", "--resume"]
          resources:
            limits:
              nvidia.com/gpu: "1"
              memory: 14Gi
              cpu: "5"
          volumeMounts:
            - { name: out, mountPath: /out }
            - { name: library, mountPath: /library, readOnly: true }
      volumes:
        - name: out
          persistentVolumeClaim:
            claimName: flyc4-out
        - name: library
          hostPath:
            path: /library
            type: Directory
      restartPolicy: Never
